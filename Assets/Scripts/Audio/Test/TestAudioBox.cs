using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestAudioBox : MonoBehaviour
{
    
    void Start()
    {
        /*AudioBox box = GetComponent<AudioBox>();
        box.PlayOneShot(SoundOneShot.ExempleSoundEffect);

        box.PlayOneShot(SoundOneShot.ExempleSoundEffect, 0);

        box.PlayLoopForTime(SoundLoop.ExempleMusic, 3);
        box.PlayLoop(SoundLoop.ExempleMusic);

        box.StopLoop(SoundLoop.ExempleMusic);

        box.PlayLoop(SoundLoop.ExempleMusic);

        box.StopLoop(SoundLoop.ExempleMusic);*/
    }
}
